pipeline {
    agent any
    
    stages {
        stage('Git checkout') {
            steps {
                git 'https://github.com/evadox/opedinas-project.git'
            } 
        }
       stage('Build') {
            steps {
               sh 'cd MywebApp && mvn clean package'
            }
        }
      stage('Test') {
            steps {
                sh 'cd MywebApp && mvn test'

            }
      }

      stage('deploy to tomcat') {
        steps {
            deploy adapters: [tomcat9(credentialsId: '55c240b0-3172-4a92-948f-d3677469d6c6', path: '', url: 'http://184.73.57.92:8080/')], contextPath: 'tomtom', war: '**/*.war'

        }

    }

   } 
}
